var colors=["blue","green","red","black"]
colors.push("orrange") // append ele to end
console.log(colors);

colors.pop()            // remove ele from end
console.log(colors);

colors.unshift("purple")   // add ele to 1st positn
console.log(colors);

colors.shift()                 // remove ele from 1st postn
console.log(colors);

console.log(colors.includes("blue"))      //check include or not
